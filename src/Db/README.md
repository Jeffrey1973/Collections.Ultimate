# Database

This folder is intended to hold SQL Server schema and migrations.

Suggested layout:
- `schema/` for the current canonical schema
- `migrations/` for incremental scripts

For now this is a placeholder to keep DB assets versioned alongside code.
