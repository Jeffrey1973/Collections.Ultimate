# Copilot Instructions

## General Guidelines
- Use singular table names in the database (e.g., 'Book', 'Household', 'LibraryItem').
- Ensure column names match Dapper queries exactly.
- For household references, use 'HouseholdId' instead of 'OwnerHouseholdId' to align with the database schema.